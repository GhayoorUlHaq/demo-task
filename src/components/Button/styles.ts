import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    buttonContainer: {
        borderRadius: 12,
        height: 60,
        width: '70%',
        backgroundColor: 'rgb(20, 77, 235)',
        justifyContent: 'center',
        alignItems: 'center'
    },
    text: {
        color: 'white',
        fontWeight: '600',
        fontSize: 22
    }
})

export default styles;
